# Prompts

1. create a read utility to read data/books.json file, and covert it to a list of Book class in book.py

2. add test case for book_reader under test folder

3. How to run and debug test_book_reader.py in Eclipse?

4. Get a list of books available in David's python bookstore.

5. create a book_repository.py, it contains BookRepository class, which uses read_book() method to load the list of books from file, and chach it to local variable.

6. create a test case of it

7. create a book_service.py, it contains BookService class, which uses BookRepository to get the list of books, and provide a method to search book by title.